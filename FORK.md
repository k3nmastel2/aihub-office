# FORK.md — aihub-office (fork of claw3d)

**Upstream:** https://github.com/iamlukethedev/claw3d (MIT, © Luke The Dev)
**Fork purpose:** the 3D virtual office for Ken's AI Hub — driven by the hub's `/api/live`
(FastAPI, `127.0.0.1:3000`) instead of an OpenClaw gateway. Studio runs on **port 3100**.
**Plan of record:** `/Users/k3n/.claude/plans/i-want-you-to-parsed-rocket.md` · **Progress:** `docs/aihub/PROGRESS.md`

## Merge policy

- `origin` = k3nmastel2/aihub-office · `upstream` = iamlukethedev/claw3d
- Pull upstream via **merge from release tags** (never rebase): `git fetch upstream && git merge <tag>`
- ALL new logic lives in our namespaces so upstream merges stay mechanical:
  - `src/lib/runtime/aihub/` — provider, snapshot, diff, http, types
  - `src/lib/aihub/` — roles, seating, serviceMap
  - `src/features/retro-office/objects/aihub/` — PodRug, DeskPaperStack, etc.
  - `src/app/api/runtime/aihub/` — hub proxy route
  - `docs/aihub/` — fork docs
- Upstream files receive **small additive edits only**, each logged below.

## Upstream files touched (keep current — conflict map for merges)

| File | Change | Phase |
|---|---|---|
| `.env` (untracked) | `PORT=3100` | 0 |
| `README.md` | attribution + fork banner at top | 0 |
| `src/features/retro-office/RetroOffice3D.tsx` | `canvasResetKey` narrowed to `remoteOfficeEnabled` only (T1 fix — was force-losing the WebGL context on every gateway-status/roster change) | 0/T1 |
| `src/lib/runtime/types.ts` | +`"aihub"` in `RuntimeProviderId` | 1 |
| `src/lib/runtime/createRuntimeProvider.ts` | +`case "aihub"` → `new AihubRuntimeProvider(client, runtimeUrl)` | 1 |
| `src/lib/gateway/GatewayClient.ts` | +`emitSyntheticEvent()`; +`"aihub"` in adapter lists (normalizeLocalGatewayDefaults, profile loop, connect probe branch → `probeAihubRuntime`, disconnect branch); +`"aihub"` in `isAutoManagedAdapter` so it auto-connects/retries on load (T8); +`probeAihubRuntime` import | 1 |
| `src/lib/studio/settings.ts` | +`"aihub"` in `StudioGatewayAdapterType` + `STUDIO_GATEWAY_ADAPTER_TYPES` + `normalizeGatewayAdapterType` + both profile loops; +`aihub` case in `resolveDefaultStudioGatewayProfile` (`DEFAULT_AIHUB_RUNTIME_URL = http://127.0.0.1:3000`); +aihub URL self-heal in `resolveStudioGatewayProfiles` (`coerceAihubProfileUrl`: a non-http(s) aihub profile URL → hub default; T13/T8b — aihub is the only adapter whose URL differs from the shared `ws://…:18789` gateway port) | 1 |
| `src/lib/studio/settings-store.ts` | +`"aihub"` in `normalizeAdapterType` | 1 |
| `src/lib/office/floors.ts` | +`FloorProvider "aihub"`, +`FloorId "aihub-live"`, +OFFICE_FLOORS `aihub-live` entry (sortOrder 29, `aihub-default`) | 1 |
| `src/features/office/components/panels/SettingsPanel.tsx` | +`["aihub","AI Hub"]` dropdown option; +aihub in `tokenOptional` + URL placeholder | 1 |
| `src/features/office/components/OfficeFloorNav.tsx` | +`aihub: "AI Hub"` in `PROVIDER_LABEL` (exhaustive `Record<FloorProvider,string>`) | 1 |
| `src/features/agents/state/store.tsx` | +`hub?: AgentHubMetadata \| null` on `AgentStoreSeed` (import from `@/lib/runtime/aihub/types`) | 1 |
| `src/features/agents/operations/agentFleetHydrationDerivation.ts` | +`hub?` on the `agents.list` entry type + `hub: agent.hub ?? null` on the derived seed (whitelist did not pass `hub` through otherwise) | 1 |
| `src/features/agents/operations/agentFleetHydration.ts` | +`role?`/`hub?` on the fetch-side `AgentsListResult.agents` type | 1 |
| `src/lib/runtime/useRuntimeConnection.ts` | +start/stop live-feed effect keyed on `[provider, status]` (via `isLiveFeedRuntimeProvider`) | 1 |
| `tests/unit/officeFloors.test.ts` | +`aihub-live` in the 3 canonical floor-list assertions (new floor) | 1 |
| `next.config.ts` | (T22/T28) prod `script-src` CSP: +`'wasm-unsafe-eval'`. three.js/drei's GLTF loader instantiates a Meshopt/Draco **WASM decoder** (`useGLTF.preload`, `furniture.tsx:754`); without `'wasm-unsafe-eval'` `WebAssembly.instantiate` was blocked in prod → repeating `CompileError` (T22) **and** an unhandled promise rejection = the zero-arg "Uncaught (in promise)" (T28). `'wasm-unsafe-eval'` allows WASM compile WITHOUT general `eval()` (strictly narrower than dev's `'unsafe-eval'`, keeps anti-XSS posture). Verified: prod rebuild → 0 rejections + 0 CompileErrors, furniture unchanged. | soak/T22+T28 |
| `src/features/retro-office/RetroOffice3D.tsx` | `syncRenderAgentUi` diffs before `setRenderAgentUiById` (was a fresh object every 250ms → re-render churn feeding update-depth loops) (T12) | 1/T12 |
| `src/features/office/tasks/useTaskBoardController.ts` | (T12) seed-cards effect depends on `standup.config` not the `standup` object; recovered-request fallback key is content-only (was hashing an advancing timestamp); **`applySharedTaskRecord`/`applyGatewayTaskRecord` are idempotent (`isSameTaskBoardCardIgnoringTimestamps`) — skip re-upsert of unchanged cards on every poll (T12c primary churn root: aihub populates the shared task store, and each `refreshSharedTasks` poll re-upserted every card → 2-3/sec state.cards churn → update-depth loop)**. **(Phase 4 QA-P1)** new `remoteTasksEnabled` param (default true); when false, `refreshRemoteTasks` no-ops + clears stale state — the aihub runtime has no gateway `tasks.*`, so fetching it threw "Gateway is not connected."/"does not implement" every poll → a red board banner + a potential unhandled rejection once the Kanban opens on the aihub floor | 1/T12, 4 |
| `src/features/office/screens/OfficeScreen.tsx` | (T12c) **gym-cooldown hardening only** (correct pure-updater fix, per team-lead): `animationNowMs` quantized to 1s buckets + `prevImmediateGymHoldRef` mutation moved out of the state updater + quantized latch `now`. NOTE: gym proved a BYSTANDER (setState *called* ~50×/cascade but always bails; the 91% stack-decode was call-frequency sampling bias), and the `officeTriggerState` value-stabilization tried here was **REVERTED** (measured-ineffective: 27/60s under active churn + JSON.stringify-per-render cost). TrailSystem guard (c92bff0) also REVERTED (churn-variance false positive). **T12 = RESOLVED (dev-only) 2026-07-10** — the update-depth loop is a `next dev` artifact (StrictMode double-invoke + dev update-depth warnings + HMR); a **production build** under fleet-scale live churn (roster 24→29, done-cycling, feed verified) shows ZERO update-depth/#185/uncaught + ZERO blackouts across mount + ~6 min feed-verified soaks. No production defect, **no code change** from the sprint (this row's gym-cooldown hardening pre-dates it, stays as shipped hygiene). The `officeTriggerState`/`animationNowMs` identity-churn is now perf/hygiene backlog, not correctness debt. Full evidence + mechanism in `docs/aihub/T12-STATIC-SWEEP.md` → "STEP-1 RESOLUTION" + PROGRESS.md. | 1/T12c |
| `src/features/retro-office/core/types.ts` | +`leavingSince?: number` on `RenderAgent` (drives the despawn opacity fade) (Phase 2) | 2 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 2) `useAgentTick` gains `spawnAtDoor`/`leavingByAgentId`/`leavingInPlaceByAgentId` params: `pickSpawnPoint` returns the aihub door for `spawnAtDoor` (aihub agents walk in vs. random teleport); a leaving-override block routes walk-out agents to `AIHUB_DOOR_EXIT` + latches `leavingSince`, flash agents freeze in place. New props `leavingByAgentId`/`leavingInPlaceByAgentId`; `spawnAtDoor = activeAdapterType === "aihub"`; `resolvedCleaningCues` now MERGES the standalone `cleaningCues` prop with animation-state cues (renderer dedups by id). Imports `objects/aihub/door`. | 2 |
| `src/features/retro-office/objects/agents.tsx` | (Phase 2) despawn fade: a leaving agent (`agent.leavingSince`) ramps body opacity to 0 over `AIHUB_LEAVING_FADE_MS` (takes precedence over the `isAway` dim), and the whole avatar group hides once faded so hub-retained done nodes don't linger as a floating nameplate. Imports `AIHUB_LEAVING_FADE_MS`. (badge billboard, nameplate chips, role props still pending — phases 4, 7) | 2 |
| `src/features/retro-office/objects/agents.tsx` | (Phase 4) blocked pulsing amber warning-triangle billboard above the head + `!` glyph; blocked amber status-dot + amber ground pulse-ring override in `useFrame`; compact nameplate chips below the plate — green task-progress pill (`remaining/total`) + blue background-task pill (count + cog-hint ring). Chips/badge are font-safe geometry+numerals (the default 3D font has no ☑/⚙/⚠ glyphs). New props `badge`/`taskChip`/`bgChip`; new `blockedBadgeRef`. (role props still pending — Phase 7) | 4 |
| `src/features/retro-office/objects/types.ts` | (Phase 4) +`badge`/`taskChip`/`bgChip` on `AgentModelProps` | 4 |
| `src/features/retro-office/core/types.ts` | (Phase 4) +optional `badge`/`badgeDetail`/`taskChip`/`bgChip`/`deskStackCount` on `OfficeAgent` (derived in `mapAgentToOffice`; omitted by non-aihub actors) | 4 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 4) `sceneAgents.map` passes `badge`/`taskChip`/`bgChip` to `AgentModel` (via `"field" in agent` guards); renders `<AihubDeskPaperStacks deskItems deskAssignmentByDeskUid agents visible={layoutPreset==="aihub"} />` right after `<AihubPodRugs>`; hovered-agent tooltip gains a `blocked` amber status pill + the hub `detail` line. Imports `AihubDeskPaperStacks` from `objects/aihub/DeskPaperStack`. **(Phase 4 fast-follow)** `openKanbanBoard` skips the claw3d TASK-MANAGER skill-install gate when `layoutPreset === "aihub"` (opens the immersive board directly) — the aihub floor feeds the board the read-only live hub-tasks source-switch, so task visibility is core and the marketplace gate is irrelevant; other floors keep the upstream gate. `layoutPreset` added to `openKanbanBoard` deps | 4 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 4) `mapAgentToOffice` derives the work-state HUD from `agent.hub` (`resolveAgentBadge`/`computeNameplateChips`/`computeDeskStackCount`); new aihub-gated `aihubTaskCardsByStatus` memo source-switches the immersive Kanban to the roster's live `tasks.items` (`buildAihubTaskCardsByStatus`) — read-only, NEVER dispatched into the shared task store (no churn/persistence corruption), off-floor passes `taskBoard.cardsByStatus` through. Imports `computeDeskStackCount`/`computeNameplateChips`/`resolveAgentBadge` + `buildAihubTaskCardsByStatus`. **(Phase 4 QA-P1)** passes `remoteTasksEnabled: shouldFetchRemoteGatewayTasks(activeAdapterType)` to `useTaskBoardController` (skips the unsupported gateway task fetch on aihub); routes the board's error prop through `resolveAihubBoardError(activeAdapterType, …)` (null on aihub — the read-only mirror has no gateway/store connection banner); the two board Refresh handlers `.catch(()=>{})` their fire-and-forget refreshes | 4 |
| `src/features/retro-office/core/furnitureDefaults.ts` | (Phase 3) +`"aihub"` in `OfficeLayoutPreset`; new `DEFAULT_AIHUB_FURNITURE` built by `buildAihubLayout` (kept rooms/kitchen/dining/lounge + 6 pods × 4 `desk_cubicle`s); `materializeDefaults` aihub branch (`_uid` = `aihub_<index>`); exports `AIHUB_POD_LAYOUTS` (rug geometry) + `AIHUB_POD_DESK_SLOTS` (seating input). Every room/appliance included verbatim so the `ensureOffice*` migrations stay no-ops and don't disturb the deterministic desk uids. Type-only import of `PodDeskSlot` from `@/lib/aihub/seating` | 3 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 2) `EMPTY_BOOLEAN_RECORD`/`EMPTY_CLEANING_CUES`/`SESSION_LEAVE_CUE_LIMIT` module consts; one memo computes `leavingByAgentId`/`leavingInPlaceByAgentId` via `resolveLeavingPlan` (client-side `firstSeenByAgentId` ref for the flash window + **`doneSinceByAgentId` ref for freshest-first walk allocation — T15**; ref-stabilized to keep agent-tick deps quiet); one `[state.agents]` effect emits session-leave janitor cues via `buildSessionGroups`/`resolveSessionLeaveCues` (guarded setState, only on new cues); passes the three new props (`cleaningCues`, `leavingByAgentId`, `leavingInPlaceByAgentId`) to RetroOffice3D. (seating memo still pending — phase 3) | 2 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 3) one aihub-gated memo (`activeAdapterType === "aihub"`) computes `aihubSeatingByDeskUid` via `computeAihubSeating(roster, AIHUB_POD_DESK_SLOTS, {firstSeenByAgentId, previousAssignment})` — reuses the Phase 2 `firstSeenByAgentIdRef`, keeps a `aihubSeatingRef` sticky across polls, ref-stabilized. The `deskAssignmentByDeskUid` prop is switched to feed this on the aihub floor (bypassing, not breaking, the manual desk picker + studio persistence); `layoutPreset` prop gains the `activeFloor.provider === "aihub" → "aihub"` branch. `EMPTY_STRING_RECORD` module const. Imports `computeAihubSeating`/`shallowEqualStringRecord` + `AIHUB_POD_DESK_SLOTS` | 3 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 3) renders `<AihubPodRugs agents deskAssignmentByDeskUid visible={layoutPreset === "aihub"} />` right after `<SceneFloorAndWalls>` (floor layer, under furniture); imports `AihubPodRugs` from `objects/aihub/PodRug` | 3 |
| `src/lib/office/floors.ts` | (Phase 3) **demo floor retired** (Ken): lobby `enabled:false` (kept in tree for mergeability, never shown/run); `DEFAULT_ACTIVE_FLOOR_ID` `"lobby"→"aihub-live"` (home floor — auto-connects + self-heals its URL); `resolveActiveOfficeFloorId` prefers the enabled default over first-enabled for a disabled/missing request | 3 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 3, demo-floor) initial `activeFloorId` state + ref = `DEFAULT_ACTIVE_FLOOR_ID` (was `"lobby"`); the runtime-floor no-URL bail targets `DEFAULT_ACTIVE_FLOOR_ID` instead of `"lobby"` (and skips when already on it, so aihub-live can't self-loop); imports `DEFAULT_ACTIVE_FLOOR_ID`. Seating memo also passes `workflow`/`group` focus keys | 3 |
| `src/lib/studio/settings.ts` | (T20) `resolveStudioGatewayProfiles` neutralizes a `lastKnownGood` whose adapter has NO enabled floor (retired, e.g. demo): `isRetiredGatewayAdapter` gates both the `selectedAdapterType` fall-through and `lastKnownGoodForSelected`, so a cold boot can't resurrect the retired demo backend behind a disabled floor when `gateway.adapterType` is absent | T20 |
| `src/features/office/components/OfficeFloorNav.tsx` | (T20) current-floor display uses `resolveActiveOfficeFloorId(activeFloorId)` instead of the old `activeIsAvailable ? activeFloorId : "lobby"` — never surfaces the now-disabled lobby (the regression symptom); imports `resolveActiveOfficeFloorId` | T20 |
| `src/lib/gateway/GatewayClient.ts` | (T20) initial `selectedAdapterType` useState default `"openclaw"→"aihub"` — first paint reads "AIHUB" instead of a misleading "OPENCLAW" transient before persisted settings load (aihub is home). Display-only: auto-connect stays gated on `settingsLoaded`; the persisted adapter still wins once settings load | T20 |
| `src/features/retro-office/core/types.ts` | (Phase 5) +`LibraryRoute` type (open-floor single-stage reading route). No `RenderAgent` field added — the library errand rides `interactionTarget: "library"` + `state` (no door stage to persist) | 5 |
| `src/features/retro-office/core/navigation.ts` | (Phase 5) re-exports `resolveLibraryRoute` + `LIBRARY_TARGET` from the new `core/navigation/libraryRoute.ts` (new aihub-owned file; single-stage walk-to for the library/research zone) | 5 |
| `src/features/retro-office/core/furnitureDefaults.ts` | (Phase 5) +`AIHUB_LIBRARY_ITEMS` (bookshelves + reading nook in the top-middle bullpen, x≈460-660/y≈45-230) spread into `AIHUB_STATIC_ITEMS`; the memory/graph/recall services' health-glow anchor + the `resolveLibraryRoute` walk-to destination. Additive, mirrors the east-lounge addition | 5 |
| `src/lib/office/places.ts` | (Phase 5) +`"library"` in `OFFICE_INTERACTION_TARGETS` (the aihub library errand's `interactionTarget`; all consumers are equality checks, no exhaustive switch) | 5 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 5) `useAgentTick` gains a trailing `libraryHoldByAgentId` param + an `explicitLibraryHold` branch in the main per-tick chain (`interactionTarget: "library"`, single-stage `resolveLibraryRoute` walk-to; the status-transition/new-actor blocks self-correct within one 250ms tick so they're untouched); `explicitLibraryHold` added to the `effectiveStatus` working condition. New props `aihubServiceErrands` (four per-target hold maps) + `aihubServicesSnapshot`; `mergeBooleanHoldMaps` OR-merges the errand overlays into `resolvedGithubReviewByAgentId` (server room), `resolvedPhoneBoothHoldByAgentId`, `resolvedQaHoldByAgentId`, and drives `resolvedLibraryHoldByAgentId` (returns the base ref unchanged when the overlay is empty → no idle churn); renders `<AihubServiceGlows>` after `<AihubDeskPaperStacks>`. Imports `resolveLibraryRoute`, `AihubServiceGlows`, `ServiceErrandHoldMaps`/`EMPTY_SERVICE_ERRAND_HOLD_MAPS`, `ServicesSnapshot`; module consts `EMPTY_HUB_SERVICES`/`EMPTY_HUB_SERVICE_LINKS` | 5 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 6) the chat slide-out swaps `<AgentChatPanel>` for `<AihubAgentCard>` when `activeAdapterType === "aihub"` (the existing click→`onAgentChatSelect`→`focusedChatAgent` selection path is reused unchanged; only the rendered panel differs). Two new handlers: `handleAihubNudge(sessionKey, message)` calls `provider.call("chat.send", …)` directly (bypasses `handleChatSend`'s office-intent parsing + the running-status enqueue → a nudge always resumes the session immediately and reports success/failure) and `handleAihubDismiss(agentId, name)` does `window.confirm` + `provider.call("agents.delete", …)` then closes the card (direct hub-hide path, not the OpenClaw studio mutation lifecycle). Imports `AihubAgentCard` | 6 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 5) subscribes to the services slice via `useServicesSnapshot()`; one aihub-gated memo computes `aihubServiceErrands` via `computeServiceErrands`/`buildServiceErrandHoldMaps` (state ref carries the ≥2-poll + 20s throttle across polls; holds ref keeps identity stable when unchanged) fed from `state.agents` `hub.currentTool` + the live `service_links`; `aihubAgentNameById` memo; passes `aihubServiceErrands` + `aihubServicesSnapshot` props to RetroOffice3D and mounts `<AihubServicesPanel>` (aihub floor only). Imports from `@/lib/aihub/serviceErrands`, `@/lib/runtime/aihub/servicesStore`, and the new panel | 5 |
| `src/features/retro-office/core/types.ts` | (Phase 7a) +optional `accentColor?: string \| null` on `OfficeAgent` (per-tool nameplate-strip accent; derived in `mapAgentToOffice`, omitted by non-aihub actors) | 7 |
| `src/features/retro-office/objects/types.ts` | (Phase 7a) +`accentColor?: string \| null` on `AgentModelProps` | 7 |
| `src/features/retro-office/objects/agents.tsx` | (Phase 7a) the nameplate left color strip paints `accentColor ?? color` — the per-tool harness accent (claude coral / codex teal / gemini blue / hermes purple) rides the strip while the per-agent `color` still drives the status dot, pulse ring, and pod-rug tint (unchanged). New destructured prop `accentColor` (default null) | 7 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 7a) `sceneAgents.map` passes `accentColor={"accentColor" in agent ? (agent.accentColor ?? null) : null}` to `AgentModel` (same `"field" in agent` guard pattern as the Phase-4 chips) | 7 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 7a) `mapAgentToOffice` derives a role via `inferRole` (aihub agents only) and applies a distinct wardrobe silhouette through `memoizedRoleWardrobe(baseProfile, role)` (referentially stable → no `appearance` churn), plus `accentColor: resolveToolAccent(hub.tool)`; both return paths now emit the wardrobe-applied `avatarProfile` + `accentColor`. Imports `inferRole`/`memoizedRoleWardrobe`/`resolveToolAccent` from `@/lib/aihub/roles` | 7 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 7b) two new props `aihubIdleGymHoldByAgentId` + `aihubPingPongPair`. `resolvedGymHoldByAgentId` becomes a memo that `mergeBooleanHoldMaps(gymBase, aihubIdleGymHoldByAgentId)` (base-ref preserved when empty → no churn) so idle agents scheduled for a workout ride the EXISTING gym route+animation. A new aihub-gated `useEffect` auto-starts a ping-pong rally for the scheduled pair — mirrors the manual `handleDeskClick` (item.type==="pingpong") field-assignment minus the camera jump; `aihubPingPongPair` re-emits each rotation bucket so a stable pair re-rallies after its self-expiring session. No new navigation | 7 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 7b) one aihub-gated `aihubIdleBehaviors` memo runs the pure `computeIdleBehaviors` over the hub-`idle` roster (minus blocked/leaving) at `animationNowMs`; two refs keep the outputs identity-stable (gym hold map via `shallowEqualBooleanRecord`; the ping-pong pair array re-emitted only on bucket/member change). Passes `aihubIdleGymHoldByAgentId` + `aihubPingPongPair` to RetroOffice3D. Imports `computeIdleBehaviors` from `@/lib/aihub/idleBehaviors`. **(Phase 7 live pass) `computeIdleBehaviors(..., { gymPercent: 0 })`** — the aihub gym workout target is UNREACHABLE via A* from the bullpen (`planPath` → empty path → gym-held agents froze at the door, and the gym↔pair rotation clobbered ping-pong), so idle agents no longer route to the gym on this layout (they lounge/roam; ping-pong now sticks). Re-enable once the aihub gym is pathable | 7 |
| `src/features/retro-office/objects/types.ts` | (Phase 7c) +`suppressBumpChatter?: boolean` on `AgentModelProps` | 7 |
| `src/features/retro-office/objects/agents.tsx` | (Phase 7c) `bumpTalking` is gated on `!suppressBumpChatter` — the random collision bubble is suppressed on the aihub floor so a bubble means real collaboration | 7 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 7c) new prop `aihubCollaborationBubbleByAgentId`; the `AgentModel` `showSpeech`/`speechText` expressions OR-in / coalesce the collaboration bubble (takes priority over stale streaming text, below standup); `suppressBumpChatter={layoutPreset === "aihub"}` threaded to `AgentModel` | 7 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 7c) aihub-gated `aihubCollaborationBubbleByAgentId` memo maps each agent's hub metadata through `resolveCollaborationBubble` (SendMessage → "💬 → recipient"); passes it to RetroOffice3D. Imports `resolveCollaborationBubble` from `@/lib/aihub/collaboration` | 7 |
| `src/features/retro-office/objects/agents.tsx` | (Phase 7d) nameplate LOD — `useFrame` now reads `state.camera`, computes camera→agent distance, and applies `resolveNameplateLod` to the nameplate `Billboard` (`nameplateGroupRef` visibility + `nameplateBgMatRef` opacity) so plates fade out toward overview / another zone. New refs + `resolveNameplateLod` import | 7 |
| `src/features/retro-office/objects/aihub/PodRug.tsx` | (Phase 7d) new `sessionRootByAgentId` prop — a pod anchored by a subagent tints by its session lead's color, so a session spanning multiple pods reads as one colored team (added to the `occupiedPods` memo deps) | 7 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 7d) per-zone camera-jump buttons (aihub-gated, below the Overview/Front-desk/Lounge row): `aihubZonePresets` memo = the four named work zones (`buildAihubNamedZonePresets`) + the six session pods (`buildAihubZonePreset` on `AIHUB_POD_LAYOUTS` centers), each sets `cameraPresetRef.current`. New prop `aihubSessionRootByAgentId` passed to `<AihubPodRugs>`. Imports `AIHUB_POD_LAYOUTS` + `buildAihubNamedZonePresets`/`buildAihubZonePreset` | 7 |
| `src/features/office/screens/OfficeScreen.tsx` | (Phase 7d) ref-stabilized aihub-gated `aihubSessionRootByAgentId` memo (`resolveSessionRootByAgentId` over the roster's `hub.parentAgentId`, only non-root entries) passed to RetroOffice3D for the PodRug uniform tint. Imports `resolveSessionRootByAgentId` | 7 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 9c) `OrbitControls` `mouseButtons.LEFT` + `enableRotate` are aihub-gated: on the aihub floor left-drag PANs the view (Ken soak #8), Space+left orbits; every other floor keeps the upstream left-drag-rotate. Zoom (wheel/DOLLY), RIGHT-pan, the camera-preset buttons (own `cameraPresetRef`), and agent click-select (R3F `onClick` only fires on non-drag clicks) are all untouched | 9 |
| `src/features/retro-office/core/furnitureDefaults.ts` | (Phase 9a/9b) aihub preset REMODEL (Ken soak #1-6): ping-pong pulled east of the QA wall so both slots are open floor (`AIHUB_LOUNGE_ITEMS`); coherent far-east lounge cluster (couches/beanbags/coffee table) + jukebox relocated to the lounge corner; kitchen kept + new break-room `AIHUB_EATING_ITEMS` (round tables/chairs); walled LIBRARY research room (`AIHUB_LIBRARY_ITEMS`, N/E/W walls + wide open south) — memory/graph/recall map here; `AIHUB_HUDDLE_ITEMS` + exported `AIHUB_HUDDLE_TABLES` anchors (huddle round tables near pods, for `computeHuddles` choreography); pod origins re-spaced (still 6×4, uids shift internally — all derived from `AIHUB_POD_DESK_SLOTS`) to keep bullpen↔zone aisles + the x1075 hall clear; phone booth moved to the lounge. **(9b)** `AIHUB_PERIMETER_ITEMS` = the south perimeter wall (furniture ⇒ `buildNavGrid` blocks it) at y746 with a single entrance door gap (x770-870) — the nav grid previously only blocked the CANVAS edge (1800×1800) while the building floor is 1800×720, so agents escaped south; y746 (vs the beige edge y720) leaves a clear grid row past the east-wing bottom wall so the far-east lounge stays reachable. New exports `AIHUB_ENTRANCE_CENTER_X`/`AIHUB_ENTRANCE_GAP_WIDTH`/`AIHUB_SOUTH_WALL_CANVAS_Y`, `AIHUB_HUDDLE_TABLES` | 9 |
| `src/features/retro-office/scene/environment.tsx` | (Phase 9b) `FloorAndWalls` gains `hideSouthWall?` — when set (aihub only) the solid environment south wall + baseboard are suppressed so agents don't visually clip it walking in/out through the new furniture entrance door ~26px further south. N/E/W environment walls unchanged | 9 |
| `src/features/retro-office/RetroOffice3D.tsx` | (Phase 9b) `<SceneFloorAndWalls hideSouthWall={layoutPreset === "aihub"} />` | 9 |

Phase 9 aihub-namespace edits (no upstream conflict): `src/features/retro-office/objects/aihub/door.ts` `AIHUB_DOOR_ENTRANCE`/`AIHUB_DOOR_EXIT` moved to y=780 (just OUTSIDE the new entrance door) so agents walk IN through the gap; `src/features/retro-office/core/navigation/libraryRoute.ts` `LIBRARY_TARGET` → (690,200) inside the new library room; `src/lib/aihub/cameraZones.ts` `AIHUB_NAMED_ZONE_ANCHORS` library/kitchen anchors re-pointed to the remodeled zones. New test `tests/unit/aihub/worldIntegrity.test.ts` (builds the real aihub nav grid: all 24 desks + library/kitchen/eating/lounge/ping-pong-slots/huddles/server reachable from the entrance; south wall's only opening is the entrance).

New aihub-owned files (Phase 5, no upstream conflict): `src/lib/aihub/serviceMap.ts`, `src/lib/aihub/serviceErrands.ts`, `src/lib/runtime/aihub/servicesStore.ts` (+ `provider.ts` publishes/reset — aihub namespace), `src/features/retro-office/objects/aihub/ServiceGlow.tsx`, `src/features/retro-office/core/navigation/libraryRoute.ts`, `src/features/office/components/panels/AihubServicesPanel.tsx`.

New aihub-owned files (Phase 7, no upstream conflict): `src/lib/aihub/roles.ts` (pure — `inferRole`/`scanRole` ported from office.js `ROLE_RULES`, role→`AgentAvatarProfile` wardrobe via `applyRoleWardrobe`/`memoizedRoleWardrobe`, per-tool `resolveToolAccent`); `src/lib/aihub/idleBehaviors.ts` (pure — `computeIdleBehaviors`: deterministic, bucket-rotating idle scheduler → a ping-pong pair when ≥2 idle + a rotating gym cohort + lounge default; `idleBehaviorPlanSignature` for identity stability); `src/lib/aihub/collaboration.ts` (pure — `resolveCollaborationBubble` (SendMessage → bubble + recipient) and `computeHuddles` (group ≥2 shared-workflow/group agents around the earliest-seen anchor; payload-ready + tested, INERT until the hub emits workflow/group, same pattern as Phase 3's focus-clustering)); `src/lib/aihub/cameraZones.ts` (pure — `buildAihubZonePreset` / `buildAihubNamedZonePresets`: per-zone camera presets from canvas anchors via `toWorld`); `src/lib/aihub/nameplateLod.ts` (pure — `resolveNameplateLod`: camera-distance → nameplate `{visible, opacity}`).

New aihub-owned files (Phase 6, no upstream conflict): `src/lib/aihub/agentCard.ts` (pure card view-model + nudge/dismiss affordance gating + read-only history rows), `src/features/office/components/panels/AihubAgentCard.tsx` (the interactions card rendered in the chat slide-out). Aihub-namespace edits (also no upstream conflict): `src/lib/runtime/aihub/types.ts` `AgentHubMetadata` gains `task`/`activity`/`activityS` (already on the normalized node) so the card renders status/age + synthesized history straight off the seed; `src/lib/runtime/aihub/snapshot.ts` `seedFromNode` copies them onto the hub seed.

Phase 6 live-pass fix (aihub namespace): `src/lib/runtime/aihub/http.ts` `postHubDismiss` now posts `{ id: nodeId }` (was `{ session_id }`) and `provider.ts` `callAgentsDelete` passes the NODE id (agentId), not `node.sessionId`. The hub's `/api/live/dismiss` hides by live node id (sessions AND subagents filtered by `id`) and 400s ("id is required") on `session_id`; this side-effecting path was never exercised before Phase 6, so the wrong field shipped latent from the Phase 1 scaffold. Nudge was already correct (`/api/live/nudge` genuinely wants `session_id` + `message`). +2 `http.test.ts` cases lock both proxy bodies.

Phase 5 live-pass fix (RetroOffice3D.tsx): `pickSpawnPoint` now jitters the aihub door spawn (±80×60px around `AIHUB_DOOR_ENTRANCE`) — the zero-jitter door made simultaneous service-errand arrivals share one nav cell and deadlock `applyAgentCollisionBumps` (d===0 clears path + retargets), freezing the walk. The four merged errand hold maps (`resolvedGithubReviewByAgentId`/`resolvedQaHoldByAgentId`/`resolvedPhoneBoothHoldByAgentId`) are memoized for identity stability. serviceMap adds `ailab`→server_room (the hub's umbrella inference-stack service id).

Update the table whenever an upstream file is touched. Rows marked _(pending)_ are planned, not yet applied.
